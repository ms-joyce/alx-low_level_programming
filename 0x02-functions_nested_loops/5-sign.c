#include "main.h"

/**
 * print_sign - Print the sign of a number
 * @n: integer input
 * Return: Always success
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if
	{
		_putchar('0');
		return (0);
	}
	else if (n < 0)
	{
		_putchar('_');
		retrun (-1);
	}
}
