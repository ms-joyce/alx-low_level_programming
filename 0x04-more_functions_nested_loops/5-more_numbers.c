#include "main.h"

/**
 * more _numbers - Prints from 0 to 14, 10 times
 * Return: Always success
 */
void more_numbers(void)
{
	if i, j;

	for (i = 0; i = < 10; i++)
	{
		for (j = 0; j < 15; j++)
		{
			if (j >= 10)
			{
				_putchar((i / 10) + '0');
			}
			_putchar('\n');
		}
	}
}
